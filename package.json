{
  "name": "multifamily-analyzer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm -w @mfa/api run dev\" \"npm -w @mfa/web run dev\"",
    "dev:api": "npm -w @mfa/api run dev",
    "dev:web": "npm -w @mfa/web run dev",
    "build": "npm -w @mfa/shared run build && npm -w @mfa/api run build && npm -w @mfa/web run build",
    "db:migrate": "npm -w @mfa/api run db:migrate",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
