server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Runtime-injected env + health check are the only unauthed paths.
  location = /env.js {
    auth_basic off;
    default_type application/javascript;
    try_files $uri =404;
  }

  location = /healthz {
    auth_basic off;
    return 200 "ok\n";
    add_header Content-Type text/plain;
  }

  # Everything else — static assets and API — is gated by basic auth.
  auth_basic "MFA";
  auth_basic_user_file /etc/nginx/.htpasswd;

  # Proxy API. Auth is handled by Express (firebaseAuth + basicAuth via
  # AUTH_MODE=both) so nginx no longer needs to gate /api/* — otherwise
  # nginx would 401-reject Bearer-token requests from talkstud.io before
  # Express ever sees them.
  #
  # Static assets below still use nginx auth_basic for the legacy
  # re.talkstud.io browser UI gate.
  location /api/ {
    auth_basic off;
    proxy_pass http://mfa-api.internal:4000;
    proxy_http_version 1.1;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Authorization     $http_authorization;
    proxy_read_timeout 60s;
  }

  # SPA — fall through to index.html for client-side routes.
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Cache hashed assets aggressively; don't cache index.html.
  location ~* \.(?:js|css|woff2?|ttf|png|jpg|jpeg|gif|svg|ico)$ {
    auth_basic "MFA";
    auth_basic_user_file /etc/nginx/.htpasswd;
    expires 30d;
    add_header Cache-Control "public, immutable";
  }
}
